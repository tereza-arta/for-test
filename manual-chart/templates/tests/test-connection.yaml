apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manual-chart.fullname" . }}-test-connection"
  labels:
    {{- include "manual-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manual-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
